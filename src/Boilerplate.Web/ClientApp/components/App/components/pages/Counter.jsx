import React from 'react';

import { Page } from '../layout';
import { Title } from '../shared';

const Counter = () => (
  <Page title="Counter">
    <Title text="Counter" />
  </Page>
);

export default Counter;
